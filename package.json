{
  "name": "@veridex/playground",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-based playground for Veridex: passkey + bounded session key + governed transfer in under 5 minutes.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@veridex/sdk": "^1.1.5",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vite-plugin-node-polyfills": "^0.28.0"
  },
  "engines": {
    "node": ">=18"
  }
}
